---
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

// 取得主分類為 vtuber 或 extraTags 包含 vtuber 的文章
const articles = await getCollection('articles', ({ data }) => 
  !data.draft && (data.tag === 'vtuber' || data.extraTags?.includes('vtuber'))
);
const sortedArticles = articles.sort((a, b) => 
  new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
---

<BaseLayout title="VTuber 專區" description="台灣 VTuber 新聞、專訪與介紹">
  <section class="page-banner" style="background-image: url('/images/banners/banner_Vtuber.jpg');">
  </section>

  <section>
    {sortedArticles.length > 0 ? (
      <div class="articles-grid">
        {sortedArticles.map((article) => (
          <ArticleCard
            title={article.data.title}
            tag={article.data.tag}
            tagName={article.data.tagName}
            date={article.data.date}
            time={article.data.time}
            author={article.data.author}
            slug={article.slug}
            image={article.data.image}
          />
        ))}
      </div>
    ) : (
      <div class="empty-state">
        <div class="empty-state__icon">🦊</div>
        <p>VTuber 文章準備中...</p>
      </div>
    )}
  </section>

  <section style="margin-top: 2rem;">
    <h2 class="section-title">💼 工商合作</h2>
    <div class="article-body" style="margin-top: 1rem;">
      <p>想要在玩報曝光嗎？我們提供 VTuber 工商文章撰寫服務！</p>
      <ul>
        <li><strong>專業文章撰寫：</strong>$1,000 TWD / 篇</li>
        <li><strong>專訪報導：</strong>$2,000 TWD 起</li>
        <li><strong>活動宣傳合作：</strong>價格另議</li>
      </ul>
      <p>聯絡我們：<a href="mailto:contact@wanbao.tw">contact@wanbao.tw</a></p>
    </div>
  </section>
</BaseLayout>
